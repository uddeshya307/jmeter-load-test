# Default values for jmeter-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

  
load_test:
  worker_pods: 3
  script: "test-influxdb-listener.jmx"

config:
  name: my-config
